# This will make sure the app is always imported when
# Django starts so that shared_task will use this app.

__version__ = '1.0.0'

# Import signals here if needed, but better to import them in apps.py